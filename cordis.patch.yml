# Relay quota indicator. Override this row's complete config in the Profile patch
# when the Sub2API-compatible provider is not named `sub2api`.
- insert:
    - id: relay-balance
      name: 'dsh-relay-balance'
      config:
        providerId: sub2api
        displayName: Relay
        usagePath: auto
        allowRemote: false
