# Relay overview. Normal users configure URL + API Key in Settings.
- insert:
    - id: relay-overview
      name: 'dsh-relay-overview'
      config:
        displayName: Relay
        baseURL: ''
        credentialRef: ''
        usagePath: auto
        allowRemote: false
